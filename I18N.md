# AndroidPlus 国际化（i18n）文档

> 多语言支持实现指南

## 📖 目录

- [概述](#概述)
- [支持的语言](#支持的语言)
- [快速开始](#快速开始)
- [使用方法](#使用方法)
- [添加新语言](#添加新语言)
- [API 参考](#api-参考)
- [最佳实践](#最佳实践)

---

## 概述

AndroidPlus v2.1 引入了完整的国际化（i18n）支持，允许用户以自己的语言使用应用程序。

### 特性

- ✅ **自动语言检测** - 根据浏览器设置自动选择语言
- ✅ **运行时切换** - 无需刷新页面即可切换语言
- ✅ **回退机制** - 缺失翻译自动回退到英语
- ✅ **本地存储** - 记住用户的语言偏好
- ✅ **易于扩展** - 简单的 JSON 格式添加新语言
- ✅ **参数化翻译** - 支持动态文本替换

---

## 支持的语言

| 语言代码 | 语言名称 | 状态 |
|---------|---------|------|
| `en` | English（英语） | ✅ 完整 |
| `zh-CN` | 简体中文 | ✅ 完整 |
| `zh-TW` | 繁體中文 | ✅ 完整 |
| `ja` | 日本語（日语） | ✅ 完整 |
| `ko` | 한국어（韩语） | ✅ 完整 |
| `es` | Español（西班牙语） | 🔄 待添加 |
| `fr` | Français（法语） | 🔄 待添加 |
| `de` | Deutsch（德语） | 🔄 待添加 |
| `ru` | Русский（俄语） | 🔄 待添加 |

---

## 快速开始

### 用户使用

1. **自动检测**
   - 打开 AndroidPlus，系统会自动检测您的浏览器语言
   - 如果支持您的语言，界面会自动显示

2. **手动切换**
   - 点击页面右上角的语言选择器（🌐）
   - 从下拉菜单选择您想要的语言
   - 界面会立即更新

3. **语言偏好保存**
   - 选择的语言会自动保存在浏览器本地存储
   - 下次访问时会记住您的选择

---

## 使用方法

### HTML 中使用

#### 1. 文本内容翻译

使用 `data-i18n` 属性：

```html
<h1 data-i18n="app.name">AndroidPlus</h1>
<p data-i18n="app.subtitle">Universal Android Web Flasher</p>
<button data-i18n="common.connect">Connect</button>
```

#### 2. HTML 内容翻译

使用 `data-i18n-html` 属性（支持 HTML 标签）：

```html
<div data-i18n-html="messages.welcome_html"></div>
```

#### 3. 属性翻译

使用 `data-i18n-title` 翻译 title 属性：

```html
<button data-i18n-title="tooltips.connect_device" data-i18n="common.connect">
    Connect
</button>
```

#### 4. 占位符翻译

```html
<input type="text" placeholder="Enter command" data-i18n="adb.command_input">
```

### JavaScript 中使用

#### 1. 获取翻译文本

```javascript
// 简单翻译
const text = window.i18n.t('common.connect');
// 结果: "Connect" 或 "连接"

// 嵌套键
const title = window.i18n.t('connection.title');
// 结果: "Device Connection" 或 "设备连接"
```

#### 2. 参数化翻译

```javascript
// 翻译键: "Device selected: {name}"
const message = window.i18n.t('messages.device_selected', {
    name: 'Redmi Note 12 Turbo'
});
// 结果: "Device selected: Redmi Note 12 Turbo"
```

#### 3. 切换语言

```javascript
// 切换到中文
await window.i18n.setLocale('zh-CN');

// 切换到英语
await window.i18n.setLocale('en');
```

#### 4. 获取当前语言

```javascript
const currentLocale = window.i18n.getLocale();
console.log(currentLocale); // "zh-CN" 或 "en"
```

#### 5. 监听语言切换事件

```javascript
document.addEventListener('localeChanged', (event) => {
    console.log('语言已更改为:', event.detail.locale);
    // 执行自定义逻辑
});
```

### 格式化工具

#### 1. 格式化日期

```javascript
const date = new Date();

// 短格式
window.i18n.formatDate(date, 'short');
// 结果: "2025/01/15" 或 "01/15/2025"

// 长格式
window.i18n.formatDate(date, 'long');
// 结果: "2025年1月15日" 或 "January 15, 2025"

// 时间
window.i18n.formatDate(date, 'time');
// 结果: "14:30" 或 "2:30 PM"
```

#### 2. 格式化数字

```javascript
// 整数
window.i18n.formatNumber(1234567);
// 结果: "1,234,567" 或 "1 234 567"

// 小数
window.i18n.formatNumber(3.14159, 2);
// 结果: "3.14" 或 "3,14"
```

#### 3. 格式化文件大小

```javascript
window.i18n.formatFileSize(1024);
// 结果: "1.00 KB"

window.i18n.formatFileSize(1048576);
// 结果: "1.00 MB"
```

---

## 添加新语言

### 步骤 1: 创建语言文件

在 `locales/` 目录下创建新的 JSON 文件，例如 `ja.json`（日语）：

```json
{
  "app": {
    "name": "AndroidPlus",
    "subtitle": "ユニバーサル Android Web フラッシャー"
  },
  "common": {
    "connect": "接続",
    "disconnect": "切断",
    "execute": "実行",
    ...
  },
  ...
}
```

### 步骤 2: 更新 i18n.js

在 `i18n.js` 的 `supportedLocales` 数组中添加新语言：

```javascript
this.supportedLocales = ['en', 'zh-CN', 'zh-TW', 'ja', 'ko', ...];
```

在 `getLocaleName()` 方法中添加语言名称：

```javascript
const names = {
    'en': 'English',
    'zh-CN': '简体中文',
    'ja': '日本語',  // 新添加
    ...
};
```

在 `detectLocale()` 方法中添加自动检测逻辑：

```javascript
// 日语
if (browserLang.startsWith('ja')) {
    return 'ja';
}
```

### 步骤 3: 更新语言选择器

在 `index.html` 的语言选择器中添加新选项：

```html
<select id="language-select" class="language-select">
    <option value="en">English</option>
    <option value="zh-CN">简体中文</option>
    <option value="ja">日本語</option>  <!-- 新添加 -->
    ...
</select>
```

### 步骤 4: 测试

1. 保存文件
2. 刷新页面
3. 选择新语言
4. 验证所有文本是否正确显示

---

## API 参考

### I18n 类

#### 构造函数

```javascript
const i18n = new I18n();
```

#### 方法

##### `t(key, params)`

获取翻译文本。

**参数:**
- `key` (string): 翻译键（支持点分隔的嵌套键）
- `params` (object, 可选): 参数对象

**返回:** string

**示例:**
```javascript
i18n.t('common.connect');
i18n.t('messages.device_selected', { name: 'Pixel 6' });
```

##### `setLocale(locale)`

切换语言。

**参数:**
- `locale` (string): 语言代码（如 'en', 'zh-CN'）

**返回:** Promise<void>

**示例:**
```javascript
await i18n.setLocale('zh-CN');
```

##### `getLocale()`

获取当前语言代码。

**返回:** string

**示例:**
```javascript
const currentLang = i18n.getLocale(); // "zh-CN"
```

##### `getSupportedLocales()`

获取支持的语言列表。

**返回:** Array<{code: string, name: string}>

**示例:**
```javascript
const languages = i18n.getSupportedLocales();
// [
//   { code: 'en', name: 'English' },
//   { code: 'zh-CN', name: '简体中文' },
//   ...
// ]
```

##### `updateUI()`

更新页面所有使用 i18n 属性的元素。

**返回:** void

**示例:**
```javascript
i18n.updateUI();
```

##### `formatDate(date, format)`

格式化日期。

**参数:**
- `date` (Date): 日期对象
- `format` (string): 格式类型 - 'short', 'long', 'time'

**返回:** string

##### `formatNumber(number, decimals)`

格式化数字。

**参数:**
- `number` (number): 数字
- `decimals` (number): 小数位数

**返回:** string

##### `formatFileSize(bytes)`

格式化文件大小。

**参数:**
- `bytes` (number): 字节数

**返回:** string

### 事件

#### localeChanged

语言切换时触发。

**事件详情:**
```javascript
{
    detail: {
        locale: 'zh-CN'  // 新语言代码
    }
}
```

**监听示例:**
```javascript
document.addEventListener('localeChanged', (event) => {
    console.log('切换到:', event.detail.locale);
});
```

---

## 最佳实践

### 1. 翻译键命名规范

使用清晰的层级结构：

```
模块.功能.具体项
```

**示例:**
```javascript
"connection.title"              // ✅ 好
"connectionTitle"               // ❌ 不好

"fastboot.flash.success"        // ✅ 好
"flashSuccess"                  // ❌ 不好

"errors.connection_failed"      // ✅ 好
"error1"                        // ❌ 不好
```

### 2. 参数化文本

对于包含动态内容的文本，使用参数：

```json
{
  "messages": {
    "device_selected": "Device selected: {name}",
    "flash_progress": "Flashing {partition}: {percentage}%"
  }
}
```

```javascript
i18n.t('messages.device_selected', { name: deviceName });
i18n.t('messages.flash_progress', { partition: 'boot', percentage: 75 });
```

### 3. 保持一致性

- 相同的操作在不同地方使用相同的翻译键
- 术语翻译保持统一（如 "Fastboot" 不要有时翻译有时不翻译）
- 标点符号遵循目标语言习惯

### 4. 提供上下文

为翻译人员添加注释：

```json
{
  "flash": "Flash",
  "_flash_comment": "动词，刷写镜像的操作"
}
```

### 5. 完整性检查

确保所有语言包包含相同的键：

```bash
# 使用工具比较语言文件
diff <(jq -S 'keys' locales/en.json) <(jq -S 'keys' locales/zh-CN.json)
```

### 6. 测试

- 在所有支持的语言中测试应用
- 检查文本溢出问题（某些语言文本更长）
- 验证特殊字符正确显示
- 测试 RTL 语言（如阿拉伯语，如需支持）

### 7. 性能优化

- 语言文件会被缓存，无需担心重复加载
- 使用懒加载，只在需要时加载语言文件
- 对于大型应用，考虑按模块分割语言文件

---

## 故障排除

### 问题 1: 翻译不显示

**症状:** 页面显示翻译键而非翻译文本

**解决方案:**
1. 检查语言文件是否正确加载
2. 验证翻译键是否存在
3. 检查浏览器控制台错误信息
4. 确认 `i18n.js` 在其他脚本之前加载

### 问题 2: 语言切换无效

**症状:** 点击语言选择器没有反应

**解决方案:**
1. 检查浏览器控制台是否有错误
2. 确认 `language-select` 元素存在
3. 验证事件监听器已绑定
4. 检查语言文件是否有语法错误

### 问题 3: 部分文本未翻译

**症状:** 某些文本仍显示英文

**解决方案:**
1. 检查对应元素是否有 `data-i18n` 属性
2. 验证语言文件中是否包含该键
3. 对于动态内容，确保调用 `i18n.t()`
4. 手动调用 `i18n.updateUI()` 刷新

### 问题 4: 参数化翻译不工作

**症状:** 参数占位符显示为 `{param}` 而非实际值

**解决方案:**
1. 检查参数名称是否匹配
2. 确保传递参数对象
3. 验证翻译文本格式正确

```javascript
// ❌ 错误
i18n.t('messages.device_selected', 'Pixel 6');

// ✅ 正确
i18n.t('messages.device_selected', { name: 'Pixel 6' });
```

---

## 贡献翻译

我们欢迎社区贡献新语言翻译！

### 贡献步骤

1. **Fork 项目**
   ```bash
   git clone https://github.com/your-username/AndroidPlus.git
   ```

2. **创建语言文件**
   ```bash
   cp locales/en.json locales/your-lang.json
   ```

3. **翻译内容**
   - 保持 JSON 结构不变
   - 只翻译值，不要修改键
   - 使用目标语言的自然表达

4. **测试翻译**
   - 在浏览器中测试所有功能
   - 检查文本长度是否合适
   - 验证特殊字符显示

5. **提交 Pull Request**
   - 标题: `feat: Add [Language] translation`
   - 描述包含:
     - 语言名称
     - 语言代码
     - 完成度百分比

### 翻译指南

- 保持应用术语一致（ADB, Fastboot, Bootloader 等）
- 使用目标语言用户熟悉的表达
- 保留品牌名称（AndroidPlus, Google, Xiaomi 等）
- 对于技术术语，提供必要的本地化
- 注意语言的正式程度（一般使用中性正式语气）

---

## 路线图

- [x] 完成繁体中文翻译
- [x] 完成日语翻译
- [x] 完成韩语翻译
- [ ] 添加更多语言（西班牙语、法语、德语、俄语）
- [ ] 实现 RTL 语言支持（阿拉伯语、希伯来语）
- [ ] 添加翻译管理后台
- [ ] 自动化翻译完整性检查

---

## 技术细节

### 语言文件格式

```json
{
  "key": "value",
  "nested": {
    "key": "value"
  },
  "parameterized": "Text with {param}"
}
```

### 自动检测优先级

1. localStorage 中保存的偏好
2. 浏览器语言设置 (`navigator.language`)
3. 默认语言（英语）

### 缓存机制

- 已加载的语言包缓存在内存中
- 语言偏好保存在 localStorage
- 刷新页面自动恢复上次选择

---

## 许可证

国际化功能遵循 AndroidPlus 的 MIT 许可证。

---

**最后更新:** 2025-01-15
